---
topic: Dynamic Table Builder — schema architecture and performance for a runtime-DDL, row-level multi-tenant, REST-only Postgres platform (Flexi)
goal: Decide the schema strategy (Runtime DDL vs Hybrid JSONB vs EAV), indexing policy, DDL safety mechanism, operational guardrails, and query layer for Flexi's Dynamic Table Builder module
type: technical
updated: 2026-08-17T22:42
---

- (event by user) Imported finished research report 'imports/report.md' (Vietnamese): Kien truc Schema va Toi uu Hieu nang cho Dynamic Table Builder. Producer: unnamed AI research tool. Production date: 2026-08-17 (today, per user). Purpose: reference only, not yet bound to a specific Flexi decision (user declined to bind it to the module's architecture spine at this time).
- (decision) Decision shape: explore (landscape survey across schema strategies), not select — report already argues for Runtime DDL + guardrails as the synthesis position rather than presenting Flexi as choosing between named candidate vendors.
- (claim) Baserow: Django backend + PostgreSQL, every user table is a real physical Postgres table, generated via Django models/migrations. status=verified
- (claim) Postgres 11+: ALTER TABLE ADD COLUMN with a non-volatile DEFAULT is metadata-only (stored in pg_attribute.attmissingval), completes in milliseconds regardless of table size — no full rewrite. status=verified
- (claim) Postgres ALTER TABLE operations that take ACCESS EXCLUSIVE lock block all reads and writes on the table, and queue behind/ahead of other statements, causing cascading blocking on busy tables. status=verified
- (claim) pg_trgm + GIN index accelerates fuzzy/substring LIKE '%x%' search in Postgres, avoiding full table scans. status=verified
- (claim) Prisma requires the schema to be known at compile-time (schema.prisma compiled into Prisma Client); it is architecturally incompatible with fully dynamic runtime-created tables without raw-SQL/schema-file workarounds. status=verified
- (claim) Knex.js provides both a query builder (DML) and a schema builder (knex.schema.alterTable etc.) for DDL, for Postgres among other dialects, usable from Node.js/NestJS. status=verified
- (claim) Specific numeric guardrails (5-10 req/s rate limits, 50k-500k row tier limits, lock_timeout=2s, statement_timeout=5s, postmortem figures like 80,000 tables / 0.5ms->200ms planning time / hundreds of GB WAL per day) carry no external citation in the source report and could not be independently verified against a named vendor's published docs or a real incident writeup within this pass; they read as plausible illustrative figures rather than confirmed facts. status=unverified
- (event) research.md assembled (7 sources, citations clean, semantic check passed, editorial polish applied); research-briefing.html rendered. status=complete
