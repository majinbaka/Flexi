import { Meta } from '@storybook/addon-docs/blocks';
import { DocumentationTable } from '../DocumentationTable';

<Meta title="Specifications/Authentication" />

# Xác thực

## Mục đích và điểm vào

Người dùng tenant đăng nhập ở `/login`; System user đăng nhập ở
`/admin/login`. Cả hai dùng `POST /api/auth/login`: sự có mặt của header
`x-tenant-id` chọn tenant login, còn không có header chọn system login.
Sau khi có access token, route nằm dưới layout yêu cầu xác thực; không có
phân quyền theo từng route ở `ProtectedRoute`.

<DocumentationTable
  columns={[
    { id: 'surface', header: 'Bề mặt', width: 'w-1/4' },
    { id: 'action', header: 'Dữ liệu / hành động', width: 'w-1/3' },
    { id: 'behavior', header: 'Hành vi quan sát được', width: 'w-5/12' },
  ]}
  rows={[
    {
      id: 'tenant-login',
      surface: 'Tenant login',
      action: 'tenant ID, email, password',
      behavior: (
        <>
          Cả ba không được rỗng tại UI; gọi login kèm <code>x-tenant-id</code>
        </>
      ),
    },
    {
      id: 'system-login',
      surface: 'System login',
      action: 'email, password',
      behavior: (
        <>
          Hai trường không được rỗng tại UI; không gửi <code>x-tenant-id</code>
        </>
      ),
    },
    {
      id: 'bootstrap',
      surface: 'Session khởi tạo',
      action: 'refresh token trong localStorage',
      behavior: (
        <>
          Refresh trước khi quyết định render route bảo vệ; sau đó gọi{' '}
          <code>me</code>
        </>
      ),
    },
    {
      id: 'logout',
      surface: 'Logout',
      action: 'refresh token hiện có',
      behavior:
        'Gọi logout nếu có token rồi luôn xoá trạng thái cục bộ, kể cả request lỗi',
    },
  ]}
/>

## Flow

1. Form trim email (và tenant ID ở tenant login), kiểm tra trường trống, rồi
   gọi `AuthContext.login`.
2. Backend xác định actor theo header, kiểm tra trạng thái actor/password và
   phát access token cùng refresh token.
3. Frontend giữ access token trong bộ nhớ, lưu refresh token vào
   `localStorage`, gọi `GET /api/auth/me`, rồi điều hướng tới `/`.
4. Một request bảo vệ nhận `401` sẽ dùng một refresh dùng chung cho các
   request đồng thời và thử lại đúng một lần. Login/refresh/logout không đi
   qua nhánh thử lại này.

## API, xác thực và lỗi

<DocumentationTable
  columns={[
    { id: 'api', header: 'API', width: 'w-1/4' },
    { id: 'guard', header: 'Guard / điều kiện', width: 'w-1/6' },
    { id: 'request', header: 'Request', width: 'w-1/4' },
    { id: 'result', header: 'Kết quả đáng chú ý', width: 'w-1/3' },
  ]}
  rows={[
    {
      id: 'login',
      api: <code>POST /api/auth/login</code>,
      guard: 'Throttle',
      request: (
        <>
          <code>{'{ email, password }'}</code>; <code>x-tenant-id</code> tùy
          loại actor
        </>
      ),
      result: (
        <>
          <code>200</code> token pair; credentials/actor inactive không hợp lệ
          trả <code>401</code>
        </>
      ),
    },
    {
      id: 'refresh',
      api: <code>POST /api/auth/refresh</code>,
      guard: 'Throttle',
      request: <code>{'{ refreshToken }'}</code>,
      result: (
        <>
          Token hợp lệ bị revoke rồi cấp cặp mới; token invalid/reused trả{' '}
          <code>401</code>
        </>
      ),
    },
    {
      id: 'logout-api',
      api: <code>POST /api/auth/logout</code>,
      guard: 'JWT',
      request: <code>{'{ refreshToken }'}</code>,
      result: (
        <>
          Chỉ revoke token thuộc <code>authAccountId</code> trong access token
        </>
      ),
    },
    {
      id: 'me',
      api: <code>GET /api/auth/me</code>,
      guard: 'JWT + permission theo actor',
      request: '—',
      result: (
        <>
          <code>Tenant</code> cần <code>auth.me.read</code>; <code>System</code>{' '}
          cần <code>system.me.read</code>
        </>
      ),
    },
  ]}
/>

`LoginDto`, `RefreshDto` và `LogoutDto` đều yêu cầu các chuỗi cần thiết; login
đòi email hợp lệ. `JwtAuthGuard` chỉ chấp nhận `Authorization: Bearer …`;
với tenant token, guard còn xác nhận tenant `ACTIVE` và đặt tenant schema vào
CLS context. `PermissionsGuard` yêu cầu toàn bộ permission gắn với route.

## Bảo quản session

Refresh token được lưu cục bộ để khôi phục session sau reload; access token
không được persist. Backend chỉ lưu SHA-256 hash của refresh token. Khi một
refresh token đã bị revoke bị tái sử dụng, toàn bộ refresh token còn sống của
chính account đó bị revoke; đây là side effect server-side, response vẫn là
`401 INVALID_REFRESH_TOKEN`.

## Yêu cầu đã xác nhận: khôi phục mật khẩu và quản trị tài khoản

Các yêu cầu trong phần này được stakeholder xác nhận cho phần mở rộng sau MVP.
Chúng chưa phải là hành vi của implementation hiện tại; các endpoint, DTO,
permission, persistence và audit tương ứng cần được triển khai trước khi dùng
như một contract production.

### Quên và đặt lại mật khẩu qua OTP

Flow đặt lại mật khẩu dùng mã OTP sáu chữ số gửi qua email, với giới hạn thử để
chống dò quét.

<DocumentationTable
  columns={[
    { id: 'api', header: 'API & request', width: 'w-1/4' },
    { id: 'guard', header: 'Guard / phân quyền', width: 'w-1/6' },
    { id: 'expiry', header: 'Thời hạn', width: 'w-1/6' },
    { id: 'audit', header: 'Audit log event', width: 'w-1/5' },
    { id: 'acceptance', header: 'Acceptance criteria', width: 'w-1/4' },
  ]}
  rows={[
    {
      id: 'forgot-password',
      api: (
        <>
          <code>POST /api/auth/forgot-password</code>
          <br />
          <code>{'{ email }'}</code>
        </>
      ),
      guard: 'Throttle: 3 request/IP/15 phút',
      expiry: 'OTP có hiệu lực 5 phút',
      audit: <code>AUTH_FORGOT_PASSWORD_REQUESTED</code>,
      acceptance: (
        <ul className="list-disc pl-4 text-xs">
          <li>
            Luôn trả <code>200 OK</code>, kể cả khi email không tồn tại, để
            chống email enumeration.
          </li>
          <li>
            Sinh OTP 6 chữ số; chỉ lưu SHA-256 hash cùng thời điểm hết hạn.
          </li>
          <li>Gửi OTP qua email hệ thống; resend cooldown là 60 giây.</li>
        </ul>
      ),
    },
    {
      id: 'reset-password',
      api: (
        <>
          <code>POST /api/auth/reset-password</code>
          <br />
          <code>{'{ email, otp, newPassword }'}</code>
        </>
      ),
      guard: 'Throttle: 5 request/IP/15 phút',
      expiry: 'Hủy OTP ngay sau khi xác thực thành công',
      audit: <code>AUTH_PASSWORD_RESET_SUCCESS</code>,
      acceptance: (
        <ul className="list-disc pl-4 text-xs">
          <li>
            Tối đa 3 lần nhập sai OTP; sau đó hủy OTP và yêu cầu lấy mã mới.
          </li>
          <li>Kiểm tra độ mạnh mật khẩu, gồm độ dài và ký tự đặc biệt.</li>
          <li>
            Revoke mọi refresh token active của tài khoản; access token hiện tại
            hết hiệu lực tối đa sau 15 phút.
          </li>
        </ul>
      ),
    },
  ]}
/>

### Quản lý session và revocation

Không dùng Redis hoặc blacklist. Việc thu hồi session chỉ revoke refresh token
trong database; access token hiện tại có thể còn hiệu lực tối đa 15 phút, cho
đến lần refresh kế tiếp.

<DocumentationTable
  columns={[
    { id: 'api', header: 'API & request', width: 'w-1/4' },
    { id: 'guard', header: 'Guard / phân quyền', width: 'w-1/6' },
    { id: 'expiry', header: 'Thời hạn tác động', width: 'w-1/6' },
    { id: 'audit', header: 'Audit log event', width: 'w-1/5' },
    { id: 'acceptance', header: 'Acceptance criteria', width: 'w-1/4' },
  ]}
  rows={[
    {
      id: 'revoke-session',
      api: <code>DELETE /api/auth/sessions/:sessionId</code>,
      guard: (
        <>
          <code>JWT</code> + <code>auth.session.manage</code>
        </>
      ),
      expiry: 'Refresh token: tức thì; access token: tối đa 15 phút',
      audit: <code>AUTH_SESSION_REVOKED</code>,
      acceptance: (
        <ul className="list-disc pl-4 text-xs">
          <li>Revoke refresh token của session được chỉ định.</li>
          <li>Chỉ hủy session của chính mình, trừ khi caller là Admin.</li>
          <li>
            Cấp lại token từ session đã hủy trả{' '}
            <code>401 INVALID_REFRESH_TOKEN</code>.
          </li>
        </ul>
      ),
    },
    {
      id: 'revoke-all-sessions',
      api: (
        <>
          <code>POST /api/auth/sessions/revoke-all</code>
          <br />
          <code>{'{ keepCurrent?: boolean }'}</code>
        </>
      ),
      guard: <code>JWT</code>,
      expiry: 'Refresh token: tức thì; access token: tối đa 15 phút',
      audit: <code>AUTH_ALL_SESSIONS_REVOKED</code>,
      acceptance: (
        <ul className="list-disc pl-4 text-xs">
          <li>Revoke mọi refresh token active của account.</li>
          <li>
            Với <code>keepCurrent=true</code>, giữ refresh token của session
            đang thực hiện request.
          </li>
        </ul>
      ),
    },
  ]}
/>

### Kích hoạt và vô hiệu hóa tài khoản

Trạng thái actor được lưu trong database và được kiểm tra reactive khi refresh
token hoặc truy vấn thông tin user.

<DocumentationTable
  columns={[
    { id: 'api', header: 'API & request', width: 'w-1/4' },
    { id: 'guard', header: 'Guard / phân quyền', width: 'w-1/6' },
    { id: 'expiry', header: 'Thời hạn tác động', width: 'w-1/6' },
    { id: 'audit', header: 'Audit log event', width: 'w-1/5' },
    { id: 'acceptance', header: 'Acceptance criteria', width: 'w-1/4' },
  ]}
  rows={[
    {
      id: 'deactivate-user',
      api: <code>PATCH /api/users/:userId/deactivate</code>,
      guard: (
        <>
          <code>JWT</code> + <code>tenant.user.manage</code> /{' '}
          <code>system.user.manage</code>
        </>
      ),
      expiry: 'Refresh token: tức thì; access token: tối đa 15 phút',
      audit: <code>ACCOUNT_DEACTIVATED</code>,
      acceptance: (
        <ul className="list-disc pl-4 text-xs">
          <li>
            Đổi actor sang trạng thái <code>INACTIVE</code>.
          </li>
          <li>Revoke mọi refresh token active của user.</li>
          <li>
            Access token cũ sống tối đa 15 phút; lần refresh hoặc login sau đó
            trả <code>401 ACTOR_INACTIVE</code>.
          </li>
        </ul>
      ),
    },
    {
      id: 'activate-user',
      api: <code>PATCH /api/users/:userId/activate</code>,
      guard: (
        <>
          <code>JWT</code> + <code>tenant.user.manage</code> /{' '}
          <code>system.user.manage</code>
        </>
      ),
      expiry: 'Tức thì',
      audit: <code>ACCOUNT_ACTIVATED</code>,
      acceptance: (
        <ul className="list-disc pl-4 text-xs">
          <li>
            Chuyển actor sang <code>ACTIVE</code> và cho phép đăng nhập bình
            thường.
          </li>
        </ul>
      ),
    },
  ]}
/>

### Account admin

Administrator có thể can thiệp tài khoản người dùng cuối để hỗ trợ kỹ thuật
hoặc xử lý vi phạm chính sách.

<DocumentationTable
  columns={[
    { id: 'api', header: 'API & request', width: 'w-1/4' },
    { id: 'guard', header: 'Guard / phân quyền', width: 'w-1/6' },
    { id: 'expiry', header: 'Thời hạn', width: 'w-1/6' },
    { id: 'audit', header: 'Audit log event', width: 'w-1/5' },
    { id: 'acceptance', header: 'Acceptance criteria', width: 'w-1/4' },
  ]}
  rows={[
    {
      id: 'admin-force-reset-password',
      api: (
        <>
          <code>POST /api/admin/users/:userId/force-reset-password</code>
          <br />
          <code>{'{ sendEmail?: boolean }'}</code>
        </>
      ),
      guard: (
        <>
          <code>JWT</code> + <code>admin.account.reset_password</code>
        </>
      ),
      expiry: 'Yêu cầu đổi mật khẩu có hiệu lực ở lần login kế tiếp',
      audit: <code>ADMIN_FORCE_PASSWORD_RESET</code>,
      acceptance: (
        <ul className="list-disc pl-4 text-xs">
          <li>
            Sinh OTP hoặc mật khẩu tạm thời và gửi qua email nếu{' '}
            <code>sendEmail=true</code>.
          </li>
          <li>
            Gán <code>mustChangePassword = true</code> trên{' '}
            <code>AuthAccount</code>.
          </li>
          <li>
            Revoke mọi refresh token hiện có; access token cũ hết hạn sau tối đa
            15 phút.
          </li>
        </ul>
      ),
    },
  ]}
/>

## Traceability

<DocumentationTable
  columns={[
    { id: 'responsibility', header: 'Trách nhiệm', width: 'w-1/3' },
    { id: 'implementation', header: 'Implementation', width: 'w-2/3' },
  ]}
  rows={[
    {
      id: 'forms',
      responsibility: 'Form và lỗi người dùng',
      implementation: (
        <>
          <code>apps/frontend/src/pages/LoginPage.tsx</code>,{' '}
          <code>AdminLoginPage.tsx</code>, <code>auth/login-error.ts</code>
        </>
      ),
    },
    {
      id: 'session',
      responsibility: 'Bootstrap/session/retry',
      implementation: (
        <>
          <code>apps/frontend/src/auth/AuthContext.tsx</code>,{' '}
          <code>lib/api-client.ts</code>
        </>
      ),
    },
    {
      id: 'endpoint',
      responsibility: 'Endpoint và DTO',
      implementation: (
        <>
          <code>apps/backend/src/modules/auth/auth.controller.ts</code>,{' '}
          <code>dto/*.dto.ts</code>
        </>
      ),
    },
    {
      id: 'logic',
      responsibility: (
        <>
          Token, actor, RBAC của <code>me</code>
        </>
      ),
      implementation: (
        <code>apps/backend/src/modules/auth/auth.service.ts</code>
      ),
    },
    {
      id: 'guards',
      responsibility: 'JWT và permission guard',
      implementation: <code>apps/backend/src/modules/auth/guards/*.ts</code>,
    },
    {
      id: 'persistence',
      responsibility: 'Persistence',
      implementation: (
        <>
          <code>apps/backend/prisma/schema.prisma</code> (
          <code>AuthAccount</code>, <code>RefreshToken</code>, actor/role
          models)
        </>
      ),
    },
  ]}
/>
