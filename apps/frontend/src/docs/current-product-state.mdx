import { Meta } from '@storybook/addon-docs/blocks';
import { DocumentationTable } from './DocumentationTable';

<Meta title="Docs/Current Product State" />

# Trạng thái sản phẩm hiện tại

> Đây là điểm bắt đầu để kiểm tra trạng thái sản phẩm hiện tại. Xác nhận thay
> đổi cụ thể bằng mã nguồn và test liên quan.

## Cách đọc bộ tài liệu

1. Đọc trang này để biết phạm vi đang chạy, phạm vi còn trống và các giới hạn
   quan trọng.
2. Xem **UI**, **Shell** và **Pages** để kiểm tra giao diện cô lập.
3. Đối chiếu hành vi nghiệp vụ/API với mã nguồn và test cùng tên trước khi vận
   hành hoặc lập kế hoạch.

## Tóm tắt theo khu vực

<DocumentationTable
  columns={[
    { id: 'area', header: 'Khu vực', width: 'w-1/6' },
    { id: 'status', header: 'Trạng thái trong mã nguồn', width: 'w-1/6' },
    { id: 'scope', header: 'Phạm vi hiện có', width: 'w-1/3' },
    { id: 'limitations', header: 'Giới hạn đáng chú ý', width: 'w-1/3' },
  ]}
  rows={[
    {
      id: 'authentication',
      area: 'Xác thực',
      status: 'Có triển khai',
      scope:
        'Đăng nhập tenant/system, JWT access + refresh rotation, logout, me, RBAC và rate limit',
      limitations:
        'Chưa có quên/đặt lại mật khẩu, quản trị role/permission, hoặc đồng bộ phiên giữa tab',
    },
    {
      id: 'tenant-onboarding',
      area: 'Onboarding tenant',
      status: 'Có triển khai phần backend và UI quản trị',
      scope:
        'Kiểm tra slug, idempotency, queue provisioning, tạo schema/metadata/seed, first admin, setup link, audit, danh sách tenant',
      limitations:
        'Email setup luôn thất bại có chủ đích vì SMTP chưa cấu hình; UI không hiển thị tiến trình/lịch sử provisioning chi tiết',
    },
    {
      id: 'dynamic-tables',
      area: 'Dynamic tables',
      status: 'Có triển khai MVP backend và UI tenant',
      scope:
        'Catalog/builder/field editor, DDL async và job polling, CRUD row có phân trang, validation runtime và quan hệ many-to-one',
      limitations:
        'API builder thật là /api/tables; GET /api/dynamic-tables vẫn là endpoint placeholder tương thích',
    },
    {
      id: 'foundation-ui',
      area: 'UI nền tảng',
      status: 'Có triển khai',
      scope:
        'Design tokens, primitive components, app shell, i18n EN/VI, login và màn hình tenant',
      limitations:
        'Chưa có frontend test runner; các story dùng dữ liệu cô lập thay vì backend thật',
    },
    {
      id: 'unimplemented-modules',
      area: 'Workflows, Pages, Cron, Mail templates, Wiki, i18n content, Settings, Logs',
      status: 'Chưa triển khai nghiệp vụ',
      scope: 'Mỗi khu vực có route backend và trang frontend nhận diện module',
      limitations:
        "Backend trả status: 'not-implemented'; frontend hiển thị PlaceholderPage",
    },
  ]}
/>

## Những route giao diện thật

- `/login` và `/admin/login`: đăng nhập tenant/system.
- `/tenants`: danh sách tenant có phân trang và bộ lọc.
- `/tenants/onboard`: tạo yêu cầu onboarding dành cho system user có quyền.
- `/dynamic-tables`: catalog, tạo bảng và chỉnh sửa field; các thao tác DDL
  được poll theo job. `/dynamic-tables/:tableId/rows`: xem, tạo, sửa và xoá
  row theo quyền.
- Các route module chưa triển khai khác hiện là `PlaceholderPage`.

## Kiến trúc lưu trữ đang chạy

Prisma sử dụng public schema cho identity và control plane: tenant, tài khoản,
role/permission, onboarding/audit và các definition liên quan. `tenantId` ở
đây biểu thị ownership/routing của bản ghi control-plane, không phải nơi chứa
dữ liệu runtime của Dynamic Tables.

Khi provisioning tenant, backend tạo schema `tenant_<tenantId>` và bootstrap
metadata `_meta_*`. Dynamic Tables chỉ truy cập schema tenant đã được xác thực
qua `TenantKnexService`; metadata runtime và các bảng do người dùng tạo đều ở
schema này.

## Traceability sau MVP

<DocumentationTable
  columns={[
    { id: 'area', header: 'Khu vực', width: 'w-1/4' },
    { id: 'evidence', header: 'Bằng chứng mã nguồn và test', width: 'w-3/4' },
  ]}
  rows={[
    {
      id: 'control-plane',
      area: 'Public control plane',
      evidence: (
        <>
          <code>apps/backend/prisma/schema.prisma</code>,{' '}
          <code>modules/auth/auth.service.ts</code> và{' '}
          <code>modules/tenants/tenants.service.ts</code>
        </>
      ),
    },
    {
      id: 'tenant-runtime',
      area: 'Tenant runtime schema',
      evidence: (
        <>
          <code>tenancy/resolve-tenant-schema.ts</code>,{' '}
          <code>tenancy/tenant-knex.service.ts</code> và{' '}
          <code>modules/tenants/provisioning.service.ts</code>
        </>
      ),
    },
    {
      id: 'dynamic-mvp',
      area: 'Dynamic Tables MVP',
      evidence: (
        <>
          <code>pages/DynamicTablesPage.tsx</code>,{' '}
          <code>pages/DynamicTableRowsPage.tsx</code>,{' '}
          <code>modules/dynamic-tables/dynamic-tables.service.ts</code> và{' '}
          <code>test/dynamic-tables.e2e-spec.ts</code> (lifecycle, relation,
          pagination, cross-tenant và guardrail)
        </>
      ),
    },
  ]}
/>

## Quy ước Storybook

Storybook là môi trường kiểm tra giao diện độc lập, không phải môi trường tích
hợp. `MockAuthProvider` và các hàm dữ liệu được truyền vào story là mock có
chủ đích để thể hiện các trạng thái UI như loading, lỗi, phân quyền và dữ liệu
rỗng. Chúng không phải bằng chứng API backend đã hoặc chưa chạy.

Để xác nhận một thay đổi UI, xem story gần component trước. Để xác nhận hành
vi nghiệp vụ/API, xem code backend và test cùng tên.
